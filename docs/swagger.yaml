openapi: 3.1.0
info:
  title: Mechanical keyboard builder api
  version: 1.0.4
  description: Api implements ...

servers:
  - url: http://localhost:3000/api/v1
    description: Local environment
    
path:
  /auth/register:
    post:
      tags:
        - Users
      summary: User registration
      operationalId: registerUser

      requestBody:
        required: true
        content:
          application/json:
            schema:


